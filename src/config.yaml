# Supported values for the task:
# "regression" - for regression (Predicting temperature)
# "classification" - for classification (Categorising Plant Type-Stage)
# "both" - for both regression and classification
task:
  "both"

